public class Stadium extends Point{
    public Stadium(String title, String info, double distanceToPrevious, double distanceToNext) {
        super(title, info, distanceToPrevious, distanceToNext);
    }
}
